#ifndef _EFFECTS_COMMEN_H_
#define _EFFECTS_COMMEN_H_
#include "cocos2d.h"
#include "appliedDifferenceNoted.h"
class knewOverheadSupplied:public appliedDifferenceNoted
{
public:
	knewOverheadSupplied();
	virtual ~knewOverheadSupplied(void);
	static knewOverheadSupplied* create(bool autoclean,bool onloadPlay = true, bool boSkillEffect = false);
	virtual bool init(bool autoclean,bool onloadPlay, bool boSkillEffect);
	void imageRevisedCaltoFront(const std::string& m_unwindingOnalthough,int maxnum,float m_nonpublicOwnsFunction,int loop,float delaytime=0.0f);
	void rePlay();
	virtual void setVisible(bool visible) override;
	void loadPlist();
	cocos2d::Animation* neededCoversInstantiating(const std::string&m_unwindingOnalthough,int maxnum,float m_nonpublicOwnsFunction);
	void moveOver(void);
	void setstartStoplightZero(int);
	bool checkShow();	
	std::string simplifyDestroyingLearn(int typeId,int plistNum, bool isOld = true);			
	CC_SYNTHESIZE(std::string, theifOfile, parametethHenceStarting);
	CC_SYNTHESIZE(std::string, m_openAdaptors, printsGenerateSearch);
	CC_SYNTHESIZE(bool, m_boHide, IsHide);
	CC_SYNTHESIZE(bool, m_whversionConventionsNeeds, exhaustedReferenceIntend);
	CC_SYNTHESIZE(bool, m_submatchSpecialize, smatchIllustratePoint);
	int setsupportedComputingRemoved(int opac,bool update = true);
	int getsupportedComputingRemoved();
	void recognizeBitsOptional();
	void reset();
	virtual void cleanup() override;
	void hierarchyPointDynamic();
	void setreviseStringWhen(cocos2d::Vec2 pos);
	virtual void setPosition(const cocos2d::Vec2 &position) override;
	virtual void setPosition(float x, float y) override;
protected:
	void circumstancesFilesInvalid(const char* fileName);	
	void attemptsForgotStrategy(cocos2d::Texture2D* a,std::string fileName);
	void determinesWitaProperties(bool suc);
private:
	bool m_userPromoted;
	bool involvedPrevents;					
	int plist_num;
	bool m_working;
	int m_maxnum;
	float m_qualifyInitially;
	int m_loop;
	float dimensionChosenOmitted;
	int m_updateSimplerLetting;				
	bool m_ischeck;																
	bool elementsStunninglySame;
	bool m_attemptParametethat;
	bool striparameterNumbers;														
	bool m_dynamicallyThat;
	bool destroyingTotal;
	std::string redefinesBetweenGrasp;
	std::map<std::string,cocos2d::EventListenerCustom*> m_seriesHappen;			
	std::vector<std::string> m_lvalueSubset;									
	std::vector<std::string> rolesNamed;
	int m_opact;
	std::string m_colonStoplight;					
	int m_memoryAbstractly;
	int typesThosmembers;
	cocos2d::Vec2 m_enforceBitvecParts;
	bool m_creatingGenerating;          
	std::string enclosedOnce; 
public:
	char programmingItasSections(float isfifth,short opposed,float full,int inform); 
	void essentiallyTest5(double extended,bool generates,short functions); 
	void allowCorrespondThought5(short bitsets); 
	float continuesRiseOlder(int readers,short compiled,unsigned short placed,int although); 
	void computingWithfifthExplain(float referring,double written,void * within,int table); 
	char readAgreeModify(char initialize,long detailed); 
	long copiedFunctionalthough(long fail,char than,float agree); 
	void visibleViceCondition(unsigned short period,bool patterns); 
private:
	float m_assumesIndicates;
	void * m_matchScopeSome;
	double m_impactEqualsConcludes;
	int m_kindCompletesWita;
};
#endif 