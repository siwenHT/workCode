#ifndef _ASYNC_FILE_OP_H__
#define _ASYNC_FILE_OP_H__
#include <string>
#include <mutex>
#include <thread>
#include <queue>
#include <string>
#include "cocos2d.h"
#include "base/CCRef.h"
NS_CC_BEGIN
class muchUlong : public Ref
{
public:
	static muchUlong* getInstance();
	void write(const char* path,const char* content, int handler);
public:
    muchUlong();
    virtual ~muchUlong();
private:
public:
protected:
	static muchUlong* m_worrySomewhat;
public:
	bool coverWcannotSold(double cannot,short referencesth); 
	bool systemNoneForestallKnow(double evaluating); 
	int specialization(long improvements); 
	void consisTimeIdentification(long resolved,char count,double chain,char designers); 
	bool descriptionOtheSuppress(double evaluated,long cerr); 
	void stunninglyCompletelyOthe(int period,long initializer,void * converted); 
private:
	void * m_contentPositionAdvanced;
	bool m_specifiersFigureHierarchy;
	double m_numberOptionsResponses;
};
NS_CC_END
#endif 
